# Changelog

Columns: `Date · time (BST)`, `AI Name (Tool)`, `Where` (Desktop / Mobile / Web — which Claude client the change was made from), `Changed` (short description with primary file(s) in backticks).

All notable changes to this repository are documented here — **newest first**. Use **BST** for the changelog.

| Date · time (BST) | AI Name | Where | Changed |
|---|---|---|---|
| 2026-05-31 | Claude Sonnet 4.6 (Claude Code) | Desktop | Fix: **charts toolbar alignment** — toolbar content was misaligned with nav/blue line; `toolbar-inner` padding corrected to `0 24px` to match `nav-inner`; `z-index` raised from 5→25 so category tooltips render above page content (`AI/charts.html`, `Biotech/charts.html`, `Defence/charts.html`). |
| 2026-05-31 | Claude Sonnet 4.6 (Claude Code) | Desktop | UX: **category filter tooltips on metrics + charts** — `CAT_TOOLTIPS` dict added to all 3 metrics pages; `tooltip` field added to `CHART_CATS` entries in all 3 charts pages; tooltip CSS (`::after/::before`) added to both page types; all filter/category buttons now have hover popups across all 6 pages. |
| 2026-05-31 | Claude Sonnet 4.6 (Claude Code) | Desktop | UX: **financial advice disclaimer moved to header** — `.header-note` strip (bordered, full width, below sector switcher) added to all 21 sector pages via `shared.css`; old `<footer>` disclaimer blocks removed from all sector pages; hub `index.html` footer unchanged (`shared.css`, all 21 sector pages). |
| 2026-05-31 | Claude Sonnet 4.6 (Claude Code) | Desktop | UX: **What-If disclaimer moved to bottom** — disclaimer moved from top of content to below the table (inside `.container`) on all 3 sectors (`AI/calculator.html`, `Biotech/calculator.html`, `Defence/calculator.html`). |
| 2026-05-31 | Claude Sonnet 4.6 (Claude Code) | Desktop | Docs: **sector AGENTS.md filter-btn spec standardised** — `Biotech/AGENTS.md` and `Defence/AGENTS.md` updated from 12px/8px 16px to 10px/5px 12px to match live standard. |
| 2026-05-31 | Claude Sonnet 4.6 (Claude Code) | Desktop | Refactor: **dashboard + charts fully dynamic** — `index.html` (all 3 sectors) now builds the stock table from `prices-data.js` via `buildTable()`; `charts.html` (all 3 sectors) builds sidebar buttons from `prices-data.js` via `buildSidebar()`; `update_prices.py` (all 3 sectors) adds `company_name` field to output and removes BeautifulSoup HTML-patching. Adding a stock now requires only: STOCKS entry in `update_prices.py`, note in `index.html` NOTES dict, and TV_MAP entry in `charts.html` if TradingView symbol differs (`AI/`, `Biotech/`, `Defence/` — `update_prices.py`, `index.html`, `charts.html`). |
| 2026-05-31 | Claude Sonnet 4.6 (Claude Code) | Desktop | Fix: **hardcoded stock counts** — `visible-count` defaults updated to use dynamic `—` placeholder; `applyFilters()` called on `DOMContentLoaded` so counts always reflect actual data; Biotech default fixed from 29 → 30 (`AI/index.html`, `Biotech/index.html`, `Defence/index.html`). |
| 2026-05-31 | Claude Sonnet 4.6 (Claude Code) | Desktop | Fix: **PSTG → P (Everpure)** — heatmap `NAMES` lookup updated (`'PSTG': 'Pure Storage'` → `'P': 'Everpure'`); charts `NO_EMBED` and button updated from `NYSE:PSTG` → `NYSE:P` (`AI/heatmap.html`, `AI/charts.html`). |
| 2026-05-31 | Claude Sonnet 4.6 (Claude Code) | Desktop | UX: **disclaimers moved to bottom** — "For research only" disclaimer now appears below content (above footer) on all news and signals pages, all 3 sectors (`AI/news.html`, `Biotech/news.html`, `Defence/news.html`, `AI/signals.html`, `Biotech/signals.html`, `Defence/signals.html`). |
| 2026-05-31 | Claude Sonnet 4.6 (Claude Code) | Desktop | UX: **filter buttons standardised** — all filter buttons across all pages set to `font-size: 10px`, `padding: 5px 12px` to match news pages (all `index.html` and `metrics.html` files). |
| 2026-05-30 | Claude Sonnet 4.6 (Claude Code) | Desktop | UX: **heatmap metric persists across sector switches** — selected timeframe (1D/1W/1M/YTD/1Y) saved to `localStorage` and restored on page load; switching sectors via the sector switcher now keeps the active metric (`AI/heatmap.html`, `Biotech/heatmap.html`, `Defence/heatmap.html`). |
| 2026-05-30 | Claude Sonnet 4.6 (Claude Code) | Desktop | Fix: **heatmap 1Y view blank** — field key was `return_1y` in the heatmap JS but `return_1yr` in `prices-data.js`; all 3 sectors affected (`AI/heatmap.html`, `Biotech/heatmap.html`, `Defence/heatmap.html`). |
| 2026-05-30 | Claude Sonnet 4.6 (Claude Code) | Desktop | UX: **metrics filter-btn padding/spacing** — padding `5px 12px → 8px 20px`, letter-spacing `1px → 2px` to match the nav bar on all 3 sectors (`AI/metrics.html`, `Biotech/metrics.html`, `Defence/metrics.html`). |
| 2026-05-30 | Claude Sonnet 4.6 (Claude Code) | Web | Fix: **signals API timeout** — changed from single 180s value to tuple `(15s connect, 120s read)` so hung TCP connections (provider accepts but never responds) now time out correctly; `python -u` flag added to both bat files for unbuffered live output (`AI/generate_signals_local.py`, `Biotech/generate_signals_local.py`, `Defence/generate_signals_local.py`, `update_all_prices.bat`, `generate_all_signals.bat`). |
| 2026-05-30 | Claude Sonnet 4.6 (Claude Code) | Web | **`generate_all_signals.bat`** — root-level script runs all 3 sector signal generators in sequence; output streams live and saves to `signals_log.txt` (overwritten each run) (`generate_all_signals.bat`). |
| 2026-05-30 | Claude Sonnet 4.6 (Claude Code) | Web | Fix: **TradingView range `'1W'` → `'5D'`** — `'1W'` is not a valid TradingView widget range; caused "Something went wrong" on all charts grids; fixed in all 3 sectors (`AI/charts.html`, `Biotech/charts.html`, `Defence/charts.html`). |
| 2026-05-30 | Claude Sonnet 4.6 (Claude Code) | Web | **Charts default to Category Grid / 1W / first category** — all 3 charts pages now open on grid view, 1-week timeframe, first category pre-selected (AI=Memory, Biotech=Large Cap, Defence=US Primes) (`AI/charts.html`, `Biotech/charts.html`, `Defence/charts.html`). |
| 2026-05-30 | Claude Sonnet 4.6 (Claude Code) | Web | Fix: **missing `</script>` tags** across 14 pages — agent that removed `buildTape()` stripped closing script tags from metrics×3, news×3, charts×3, calculator×3, index×2; caused blank pages on all affected views. |
| 2026-05-30 | Claude Sonnet 4.6 (Claude Code) | Web | **PSTG → P** — Pure Storage rebranded to Everpure (NYSE: P, Apr 2026); updated ticker in `AI/update_prices.py`, `AI/index.html`, `AI/metrics.html`, and `prices-data.js`. |
| 2026-05-30 | Claude Sonnet 4.6 (Claude Code) | Web | **`update_all_prices.bat`** — root-level batch file runs all 3 sector price updates in sequence; output logged to `update_log.txt` (overwritten each run) via PowerShell `Tee-Object` so log is readable without copy-pasting (`update_all_prices.bat`). |
| 2026-05-30 | Claude Sonnet 4.6 (Claude Code) | Web | Fix: **COAG missing from Biotech dashboard** — Hemab Therapeutics was in the ticker list and `prices-data.js` but had no table row; added to Rare Disease section (`Biotech/index.html`). Now correctly shows 30 of 30 stocks. |
| 2026-05-30 | Claude Sonnet 4.6 (Claude Code) | Web | Fix: **Biotech media query 900px → 768px** — `Biotech/index.html` was the only file using a 900px breakpoint; at split-screen widths fonts shrank while AI/Defence stayed full-size (`Biotech/index.html`). |
| 2026-05-30 | Claude Sonnet 4.6 (Claude Code) | Web | **Dashboard headers** — sector name now the large h1 ("AI Infrastructure", "Biotech", "Defence & Aerospace"); "Stock Universe" moved to small monospace label above; Biotech h1 split: "Bio" dark / "tech" blue accent (all 3 `index.html` files). |
| 2026-05-30 | Claude Sonnet 4.6 (Claude Code) | Web | **Best-practice refactor** — `shared.js` created (pulls `toggleTheme()` + `buildTape()` out of 22 inline copies); `.disclaimer` CSS moved to `shared.css`; `prices-data.js` moved from `<head>` to `<body>`; `<meta name="description">` added to all 22 pages; −992 lines of duplicate code (`shared.js`, `shared.css`, all HTML files). |
| 2026-05-29 | Claude Sonnet 4.6 (Claude Code) | Web | **Light/dark mode toggle — full rollout** — ☾/☀ button in header sector-switcher on all 22 pages (root hub + AI×7 + Biotech×7 + Defence×7); light mode default (`:root`); dark mode via `html[data-theme="dark"]`; `localStorage` persistence; flash-prevention script in every `<head>`; heatmap D3 colours driven by CSS vars (`getCssVar()`) with re-render on toggle; `--muted`/`--slate` darkened in light mode; hardcoded colour fixes throughout (`shared.css`, `index.html`, all sector pages). |
| 2026-05-28 21:56 | Claude Sonnet 4.6 (Claude Code) | Web | **Biotech filter buttons standardised** — padding, letter-spacing, text-transform, font-weight, transition now match AI and Defence exactly; responsive override added (`Biotech/index.html`). |
| 2026-05-28 21:23 | Claude Sonnet 4.6 (Claude Code) | Web | **Sector switcher replaces back-link** across all 21 sector pages — mini cards for all 3 sectors in header right side; active sector bright with cyan top border, others dim; links are context-aware (stay on same page type when switching); Stock Hub card links back to root `index.html`; `shared.css` updated with `.sector-switcher`, `.sector-card`, `.hub-card` styles; no `.back-link` HTML or CSS remains (`AI/*.html`, `Biotech/*.html`, `Defence/*.html`, `shared.css`). |
| 2026-05-28 20:35 | Claude Sonnet 4.6 (Claude Code) | Web | **shared.css** — extracted all shared CSS (reset, body, ticker tape, header, nav, footer, container) from all 21 sector pages into a single `shared.css`; Google Fonts loaded via `<link>` tag (not `@import` — needed for `file://` compatibility); change pills and notes-text unified to single spec across all dashboards (`shared.css`, all 21 sector page `<style>` blocks). |
| 2026-05-28 18:41 | Claude Sonnet 4.6 (Claude Code) | Web | **Defence & Aerospace sector launched** — 28 stocks across 6 categories (US Primes, UK/European, Cyber/Intel, Drones, Space, Weapons/Systems); full suite of 7 pages (dashboard, metrics, news, signals, heatmap, charts, calculator); prices via GitHub Actions; signals via OpenRouter DeepSeek; LSE stocks (BA.L, RR.L, QQ.L) divided by 100 for GBP (`Defence/`, `index.html`, `.github/workflows/`). |
| 2026-05-27 23:29 | Claude Sonnet 4.6 (Claude Code) | Web | **Rank delta badges** on AI and Biotech signals pages — show movement since last run (▲↑↓▼ with colour); signals schedule updated to Mon/Wed/Fri; "How It Works" section added to signals pages (`AI/signals.html`, `Biotech/signals.html`). |
| 2026-05-27 22:31 | Claude Sonnet 4.6 (Claude Code) | Web | **Web search plugin** added to AI and Biotech signals generators — DeepSeek can pull live news during signal generation via OpenRouter web search tool (`AI/generate_signals_local.py`, `Biotech/generate_signals_local.py`). |
| 2026-05-27 22:44 | Claude Sonnet 4.6 (Claude Code) | Web | Fix: **filter button tooltips** now appear above buttons (not clipped below nav) on all sector dashboards — `bottom: calc(100% + 10px)` positioning (`AI/index.html`, `Biotech/index.html`). |
| 2026-05-27 20:49 | Claude Sonnet 4.6 (Claude Code) | Web | **Category filter tooltips** on all sector news pages — hover on category filter buttons to see descriptions; **nav link tooltips** added to Biotech dashboard (`AI/news.html`, `Biotech/news.html`, `Biotech/index.html`). |
| 2026-05-26 01:55 | Claude Sonnet 4.6 (Claude Code) | Web | **All dashboard columns sortable** — 52-Week Range and Key Angle columns now sortable on both AI and Biotech dashboards (`AI/index.html`, `Biotech/index.html`). |
| 2026-05-26 01:54 | Claude Sonnet 4.6 (Claude Code) | Web | Fix: **Company column sortable** on Biotech and AI metrics tables (`AI/metrics.html`, `Biotech/metrics.html`). |
| 2026-05-26 01:52 | Claude Sonnet 4.6 (Claude Code) | Web | Fix: stale tooltip in Biotech nav — replaced 'AI top 10 picks' with 'Top 10 biotech picks' (`Biotech/signals.html`, `Biotech/news.html`, `Biotech/heatmap.html`, `Biotech/charts.html`, `Biotech/calculator.html`, `Biotech/metrics.html`). |
| 2026-05-26 00:42 | Claude Sonnet 4.6 (Claude Code) | Web | Fix: Biotech signals page h1 read 'AI Signals' → corrected to 'Biotech Signals' (`Biotech/signals.html`). |
| 2026-05-25 13:45 | Claude Sonnet 4.6 (Claude Code) | Web | Added **COAG** (Hemab Therapeutics) to Biotech sector — 30 stocks total (`Biotech/update_prices.py`, `Biotech/index.html`, `Biotech/news.html`). |
| 2026-05-24 21:36 | Claude Sonnet 4.6 (Claude Code) | Web | Built **Biotech sector dashboard** — 29 stocks across 9 categories (large-cap, UK listed, gene editing, genomics, oncology, mRNA, rare disease, metabolic, neuroscience); full suite: metrics, news, signals, heatmap, charts, calculator; prices via GitHub Actions; `Biotech/` subfolder + workflow updated (`Biotech/`, `.github/workflows/update-prices.yml`, `.github/workflows/generate-signals.yml`, `index.html`, `FEATURES.md`). |
| 2026-05-24 16:41 | Claude Sonnet 4.6 (Claude Code) | Web | Heatmap: added prices **last-updated timestamp** to toolbar (`AI/heatmap.html`). |
| 2026-05-24 | Claude Sonnet 4.6 (Claude Code) | Web | **Full migration from KobiLDN/aiSTOCKS** — copied all AI sector files into `AI/`; moved `.github/workflows/` to repo root with `working-directory: AI` on all steps; built hub landing page (`index.html`) with sector cards; added `← All Sectors` back link to all 7 AI pages; added root `.gitignore`; repo docs created (`AGENTS.md`, `WORKFLOW.md`, `CHANGELOG.md`, `FEATURES.md`). v1. |
