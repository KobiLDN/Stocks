# Changelog

Columns: `Date · time (BST)`, `AI Name (Tool)`, `Where` (Desktop / Mobile / Web — which Claude client the change was made from), `Changed` (short description with primary file(s) in backticks).

All notable changes to this repository are documented here — **newest first**. Use **BST** for the changelog.

| Date · time (BST) | AI Name | Where | Changed |
|---|---|---|---|
| 2026-06-05 | Claude Sonnet 4.6 (Claude Code) | Web | UX: **portfolio heatmap split — gainers left, losers right (local)** — holdings treemap replaced with two side-by-side Chart.js treemap instances; gainers sorted by 1D% desc (highest first), losers sorted by 1D% asc (worst on far right); halves sized proportionally by holding count; each half hidden if empty (`portfolio.py`). |
| 2026-06-05 | Claude Sonnet 4.6 (Claude Code) | Web | UX: **portfolio.html light mode + dark toggle (local)** — light mode is now the default; `html[data-theme="dark"]` overrides to dark palette; flash-prevention script in `<head>`; ☾ Dark / ☀ Light button top-right; preference persisted in `localStorage` (`portfolio.py`). |
| 2026-06-05 | Claude Sonnet 4.6 (Claude Code) | Web | UX: **portfolio chart shows positions value (local)** — "Portfolio Value Over Time" now plots `total_value − free_cash` (stock holdings only) vs cost basis; excludes idle cash so the gap = unrealised P&L (`portfolio.py`). |
| 2026-06-05 | Claude Sonnet 4.6 (Claude Code) | Web | Fix: **portfolio ppl frozen across history records (local)** — `total_ppl` was sourced from `cash.get("result")` (T212 cash endpoint, appears static); now summed from per-position `ppl` values in `/equity/portfolio` response so it updates live each run (`portfolio.py`). |
| 2026-06-05 | Claude Sonnet 4.6 (Claude Code) | Web | Fix: **portfolio.py works without local STOCKSMain clone (local)** — `load_export()` falls back to fetching manifest + snapshot from `stocks-4qw.pages.dev`; vol data sourced from export JSON when no local prices-data.js files present; enables running on any PC with only Python + httpx + `.key` (`portfolio.py`). |
| 2026-06-05 | Claude Sonnet 4.6 (Claude Code) | Web | Fix: **T212 Basic auth in MCP server + export script** — `t212_mcp/server.py` and `generate_export.py` were using wrong auth format (`Authorization: <raw_key>` Bearer-style); fixed to split `KEY_ID:SECRET_KEY` and pass as `httpx auth=(key_id, secret)` (HTTP Basic auth as T212 API requires) (`t212_mcp/server.py`, `generate_export.py`). |
| 2026-06-04 | Claude Sonnet 4.6 (Claude Code) | Desktop | Infra: **portfolio output files renamed (local)** — `snapshot.html` → `portfolio.html`, `analysis.json` → `portfolio_analysis.json`; all references updated in `portfolio.py`; existing files renamed in place (`t212_mcp/portfolio.py`, `t212_mcp/portfolio.html`, `t212_mcp/portfolio_analysis.json`). |
| 2026-06-04 | Claude Sonnet 4.6 (Claude Code) | Desktop | Fix: **Crypto Yahoo Finance links** — all pages now use `ticker-USD` format (e.g. `XRP-USD` not `XRP`); special cases hardcoded: `TON→TON11419-USD`, `SUI→SUI20947-USD`, `PEPE→PEPE24478-USD`; `Crypto_update_prices.py` yfinance tickers corrected for TON, SUI, PEPE; `CRYPTO_YF` map in `index.html`, `metrics.html`, `news.html`, `heatmap.html`. |
| 2026-06-04 | Claude Sonnet 4.6 (Claude Code) | Desktop | UX: **header-sub standardised** — removed `Last updated` from all 6 sector dashboard header-sub lines (now in data bar only); removed local `font-size: 12px` overrides from `AI/index.html` + `Defence/index.html`; all sector pages now use shared.css `header-sub` (13px IBM Plex Mono) consistently. |
| 2026-06-04 | Claude Sonnet 4.6 (Claude Code) | Desktop | Fix: **charts pages max-width 1600px → 1920px** — `.toolbar-inner` and grid containers were 1600px while nav is 1920px; toolbar/chart grid now align with nav buttons on wide screens. All 6 `charts.html` files. |
| 2026-06-04 | Claude Sonnet 4.6 (Claude Code) | Desktop | UX: **compact header-note disclaimers** — all 42 pages reduced to single-line variants: general · metrics (Yahoo Finance/yfinance) · news (VADER headline scoring) · signals (AI-generated) · calculator (YTD from 1 Jan 2026). |
| 2026-06-04 | Claude Sonnet 4.6 (Claude Code) | Desktop | UX: **data bar on every page** — `buildDataBar()` in `shared.js` auto-injects after nav into `.container` (or `<main>` on charts); shows "Prices updated: [ts] · Auto-refreshed Mon–Fri · 09:00, 15:30, 21:30 BST" on price pages; signals pages keep their own `refresh-banner` (skipped); 16px equal spacing above and below (`shared.js`, `shared.css`). |
| 2026-06-04 | Claude Sonnet 4.6 (Claude Code) | Desktop | UX: **signals refresh-banner shows time** — "Auto-refreshed Mon / Wed / Fri · 08:00 BST (07:00 UTC) · GitHub Actions" added to all 6 `signals.html` in-page banners. |
| 2026-06-04 | Claude Sonnet 4.6 (Claude Code) | Desktop | Infra: **external cron trigger via cron-job.org** — 3 cronjobs created (08:00, 14:30, 20:30 UTC weekdays) hitting GitHub API `POST /repos/KobiLDN/Stocks/actions/workflows/update-prices.yml/dispatches`; replaces unreliable GitHub native scheduler which was delaying runs by 2–3h; token scoped to `repo, workflow`; GitHub cron entries kept as fallback (`cron-job.org`, `.github/workflows/update-prices.yml`). |
| 2026-06-04 | Claude Sonnet 4.6 (Claude Code) | Desktop | Fix: **hub index.html works on `file://`** — all 6 sector `prices-data.js` + `signals-local.js` preloaded as `<script>` tags and snapshotted to `window.__pd_*` / `window.__sd_*` globals; `fetchPriceMap`/`fetchSector` use preloaded globals first (works on `file://`), fall back to `fetch()` on HTTP (`index.html`). |
| 2026-06-04 | Claude Sonnet 4.6 (Claude Code) | Desktop | Infra: **robust push retry in all 3 workflows** — replaced `git pull --rebase && git push` with explicit `git fetch origin main; git rebase origin/main; git push origin HEAD:main` in a 3-attempt loop (10s sleep); prevents transient non-fast-forward failures (`update-prices.yml`, `generate-signals.yml`, `generate-export.yml`). |
| 2026-06-04 | Claude Sonnet 4.6 (Claude Code) | Desktop | Fix: **heatmap blank tiles — SVG clip-path ID conflict** — `gDiv`/`lDiv` in `drawSplit` had no `id`; all treemaps on the page generated clip-paths `c-0`, `c-1`… which conflict globally (SVG clip-path IDs are document-scoped); tiles in earlier-rendered treemaps were clipped to wrong rectangles; fix: assign `(parentId)-g` and `(parentId)-l` so every clip-path ID is unique. All 6 heatmap files. |
| 2026-06-04 | Claude Sonnet 4.6 (Claude Code) | Desktop | Fix: **heatmap blank tiles — explicit width for split sub-divs** — `drawTreemap()` called `containerEl.clientWidth` on newly-created `gDiv`/`lDiv` before browser layout; returned 0 → SVG built at 1200px fallback → tiles positioned outside visible area; fix: `explicitWidth` param added; `drawSplit()` passes `gW`/`lW` directly. All 6 heatmap files. |
| 2026-06-04 | Claude Sonnet 4.6 (Claude Code) | Desktop | UX: **heatmap by-sector view groups gainers left, losers right** — `renderSectors()` now calls `drawSplit()` on every category block; all sector sub-maps show green tiles left / red tiles right. All 6 heatmap files. |
| 2026-06-04 | Claude Sonnet 4.6 (Claude Code) | Desktop | UX: **heatmap flat solid colours** — removed gradient shading; gainer tiles solid `var(--green)`, loser tiles solid `var(--red)`, neutral `var(--surface2)`; `tileColor()` simplified. All 6 heatmap files. |
| 2026-06-04 | Claude Sonnet 4.6 (Claude Code) | Desktop | UX: **heatmap All Stocks view groups gainers left, losers right** — `drawSplit()` helper splits stocks by sign into two side-by-side treemaps sized proportionally by stock count; `renderAll()` and `renderSectors()` both use it. All 6 heatmap files. |
| 2026-06-04 | Claude Sonnet 4.6 (Claude Code) | Desktop | Fix: **header-note alignment with blue bar** — `.header-note` given `width: calc(100% - 48px)` + `max-width: 1872px` + `margin: 8px auto 0`; left edge now aligns with the blue accent bar and nav content on all screen sizes (`shared.css`). |
| 2026-06-04 | Claude Sonnet 4.6 (Claude Code) | Desktop | UX: **portfolio snapshot dashboard overhaul (local)** — sector allocation donut replaced with variable-height equal-width coloured blocks (biggest tallest, sits at bottom, 1D weighted-avg % floats above each block in green/red); layout restructured to right-col with sector blocks full-width on top + chart/heatmap side by side below; Real P&L (GBP) card added showing `total_value − free_cash − invested` to reveal FX drag vs T212's USD-based P&L; black text on all sector blocks; dark accent top border per block (`t212_mcp/portfolio.py`). |
| 2026-06-04 | Claude Sonnet 4.6 (Claude Code) | Desktop | Infra: **price updates 2× → 3× daily** — `update-prices.yml` schedule changed to 08:00 UTC (09:00 BST, 1h after UK open), 14:30 UTC (15:30 BST, 1h after US open), 20:30 UTC (21:30 BST, after US close); weekdays only; cost remains $0 (yfinance + public repo Actions). |
| 2026-06-04 | Claude Sonnet 4.6 (Claude Code) | Desktop | UX: **last-updated date moved to page header** — "Last updated [date]" now appears next to `v1 · Select a sector` in the hub header, sourced from the most recent `prices-data.js` across all sectors; removed from Top 20 panel subtitle (`index.html`). |
| 2026-06-04 | Claude Sonnet 4.6 (Claude Code) | Desktop | Fix: **BST timestamps on exports page** — `exports/index.html` converts UTC `generated` field to BST/GMT in browser; works retroactively on all existing snapshots. `generate_export.py` now stamps new exports as BST/GMT directly (`exports/index.html`, `generate_export.py`). |
| 2026-06-04 | Claude Sonnet 4.6 (Claude Code) | Desktop | Fix: **Top 20 ticker deduplication** — same ticker (e.g. MU) could appear in multiple sector signals; now deduplicates by ticker keeping highest confidence, guarantees exactly 20 unique stocks (`index.html`). |
| 2026-06-03 | Claude Sonnet 4.6 (Claude Code) | Desktop | UX: **hub Top 20 Signals panel + layout overhaul** — full-width "Top 20 Signals" panel above sector cards aggregates all picks across all 6 sectors, ranks by confidence desc, sortable on all 9 columns (▼/▲ indicator, numeric data-val sort, STRONG BUY above BUY); sector cards changed from 2-col to 3-col grid (3+3 layout); header/main/footer all set to 1920px max-width; responsive breakpoints: 2-col ≤ 1100px, 1-col ≤ 640px; card gap increased to 28px (`index.html`). |
| 2026-06-03 | Claude Sonnet 4.6 (Claude Code) | Desktop | UX: **portfolio.py dashboard overhaul** — `snapshot.html` rebuilt with: squarified treemap heatmap (chartjs-chart-treemap, tiles sized by 1D% magnitude, coloured by direction); sector allocation donut (Chart.js, vivid per-sector colours); portfolio value over time line chart (history appended to `portfolio_history.json` each run); Today's Movers card (best/worst 1D from holdings); Holdings table gains Bought + Current columns; 1920px max-width wrapper; responsive flex layout (cards → donut → chart → heatmap); `TICKER_ALIASES` dict maps T212 non-standard tickers to universe tickers (e.g. HUTMF → HUT). |
| 2026-06-03 | Claude Sonnet 4.6 (Claude Code) | Desktop | Infra: **vol_1d / vol_1w / vol_1m added to export** — `generate_export.py` PRICE_FIELDS updated; fields will appear in `exports/YYYY-MM-DD.json` from next GitHub Actions run (`generate_export.py`). |
| 2026-06-02 | Claude Sonnet 4.6 (Claude Code) | Desktop | Infra: **local portfolio snapshot script** — `G:\My Drive\coding\ai\Stocks\t212_mcp\portfolio.py` + `run.bat`; fetches live T212 positions via Basic auth (`KEY_ID:SECRET_KEY`), merges with latest export from STOCKSMain, writes `snapshot.json` (paste to Claude) + `snapshot.txt` (paste to DeepSeek); key lives outside any git repo, never committed; `AGENTS.md` + `FEATURES.md` updated to document T212 Basic auth format and container-level script. |
| 2026-06-02 | Claude Sonnet 4.6 (Claude Code) | Web | Docs: **MDs updated** — FEATURES.md, CHANGELOG.md, AGENTS.md updated for export pipeline + T212 MCP server. |
| 2026-06-02 | Claude Sonnet 4.6 (Claude Code) | Web | Infra: **T212 MCP server + local portfolio export** — `t212_mcp/server.py` (FastMCP, 4 tools: `get_positions`, `get_account_summary`, `get_orders`, `get_portfolio_vs_signals`); `.claude/settings.json` wires it into Claude Code; `generate_export.py` fetches T212 positions when key present and writes `YYYY-MM-DD-portfolio.json` locally — gitignored, never committed, never online (`t212_mcp/`, `.claude/settings.json`, `generate_export.py`, `.gitignore`). |
| 2026-06-02 | Claude Sonnet 4.6 (Claude Code) | Web | Infra: **dated snapshots + manifest + date selector** — `generate_export.py` now writes `exports/YYYY-MM-DD.json+csv` and updates `exports/manifest.json`; export page loads manifest on init, builds date dropdown, fetches selected snapshot on change with client-side caching (`generate_export.py`, `exports/index.html`, `exports/manifest.json`). |
| 2026-06-02 | Claude Sonnet 4.6 (Claude Code) | Web | Infra: **daily export workflow** — `generate_export.py` at repo root combines all 6 sectors into `all_stocks.csv` + `all_stocks.json` (176 stocks; metrics, what-if £100/£1k/£10k × 5 timeframes, top-3 news, signals); `generate-export.yml` runs at 22:00 UTC weekdays and commits output; files served via Cloudflare Pages (`generate_export.py`, `.github/workflows/generate-export.yml`, `all_stocks.csv`, `all_stocks.json`, `.gitignore`). |
| 2026-06-02 | Claude Sonnet 4.6 (Claude Code) | Web | Docs: **all MDs updated** — `FEATURES.md` moved Energy + 1D/1W/1M + heatmap stroke + 1920px to Done; `CHANGELOG.md` entries added; `README.md` sectors table + repo tree updated to show all 6 sectors, shared.css comment 28→42 pages, WORKFLOW.md link fixed (`AGENTS.md`, `FEATURES.md`, `CHANGELOG.md`, `README.md`). |
| 2026-06-02 | Claude Sonnet 4.6 (Claude Code) | Web | Style: **site max-width 1600→1920px** — `.header-inner`, `.nav-inner`, `.container` max-width widened from 1600px to 1920px in `shared.css`; all 42 sector pages benefit automatically (`shared.css`). |
| 2026-06-02 | Claude Sonnet 4.6 (Claude Code) | Web | **Energy sector launched** — 20 stocks across 5 categories (Oil Majors: XOM/CVX/SHEL/BP/TTE/EQNR; E&P/Refining: COP/OXY/EOG/MPC/VLO; Oilfield Services: SLB/HAL/BKR; Utilities: NEE/SSE.L; Clean Energy: ENPH/FSLR/BEPC/DNNGY); full 7-page suite; Energy card added to all 35 existing sector pages; hub card + SECTORS array; both workflows updated (`Energy/`, `index.html`, `.github/workflows/update-prices.yml`, `.github/workflows/generate-signals.yml`). |
| 2026-06-02 | Claude Sonnet 4.6 (Claude Code) | Web | Style: **heatmap text stroke removed** — SVG stroke/stroke-width/stroke-linejoin/paint-order removed from ticker and value text on all 5 sector heatmaps; tile border (0.5px) retained (`AI/heatmap.html`, `Biotech/heatmap.html`, `Defence/heatmap.html`, `Tech/heatmap.html`, `Crypto/heatmap.html`). |
| 2026-06-02 | Claude Sonnet 4.6 (Claude Code) | Web | UX: **1D / 1W / 1M columns on metrics** — three sortable return columns added between YTD and Beta on all 4 equity metrics pages; colour-coded like YTD; sort keys handle null values gracefully (`AI/metrics.html`, `Biotech/metrics.html`, `Defence/metrics.html`, `Tech/metrics.html`). |
| 2026-06-02 | Claude Sonnet 4.6 (Claude Code) | Web | Docs: **FEATURES.md updated** — Energy sector backlog expanded with full 20-stock / 5-category spec; Global Mega-Cap Leaders (50 stocks, 7 categories) added to backlog (`FEATURES.md`). |
| 2026-06-02 | Claude Sonnet 4.6 (Claude Code) | Web | Docs: **AGENTS.md + WORKFLOW.md updated for web Claude workflow** — removed two-clone STOCKSMain/STOCKSDev folder references; added prominent branch rules (dev only, ask before pushing to main); Crypto listed as live sector; Energy as next in backlog (`AGENTS.md`, `WORKFLOW.md`). |
| 2026-06-02 | Claude Sonnet 4.6 (Claude Code) | Web | Crypto: **ETF data purged from committed cache files** — removed UNI/AAVE/ARB/OP/SHIB/COIN/MSTR/IBIT from `prices-data.js` and `prices.json`; added stubs for XLM/SUI/HBAR/TON/ALGO/MINA/PEPE; replaced old miner signal picks (HUT/RIOT/CLSK/IREN/CIFR) in `signals-local.json` and `signals-local.js` with placeholder crypto picks (`Crypto/prices-data.js`, `Crypto/prices.json`, `Crypto/signals-local.json`, `Crypto/signals-local.js`). |
| 2026-06-02 | Claude Sonnet 4.6 (Claude Code) | Web | **Crypto universe revised to 19 specific coins** — user-specified list replacing previous 20-asset universe; new categories: bitcoin / layer1 (ETH,SOL,BNB,ADA,AVAX) / payments (XRP,XLM,TRX) / emerging (SUI,HBAR,TON,ALGO,MINA) / infra (MATIC,DOT,LINK) / meme (DOGE,PEPE); all 7 pages + hub + Python scripts updated (`Crypto/`, `index.html`, `CHANGELOG.md`, `FEATURES.md`). |
| 2026-06-02 | Claude Sonnet 4.6 (Claude Code) | Web | **Crypto sector rebuilt as real coins** — replaced 16 crypto-adjacent equities (ETFs, miners, exchanges) with 20 actual crypto assets: BTC; Layer 1 (ETH, SOL, BNB, XRP, ADA, AVAX, DOT, TRX); DeFi (LINK, UNI, AAVE); Layer 2 (MATIC, ARB, OP); Meme (DOGE, SHIB); plus 3 crypto equities (COIN, MSTR, IBIT); all 7 Crypto pages updated with new categories, TradingView symbols, and coin descriptions; `Crypto_update_prices.py` and `Crypto_generate_signals_local.py` updated (`Crypto/`, `CHANGELOG.md`, `FEATURES.md`). |
| 2026-06-02 | Claude Sonnet 4.6 (Claude Code) | Web | Infra: **sector selector on update-prices workflow** — `workflow_dispatch` now has a dropdown (all / AI / Biotech / Defence / Tech / Crypto) so a single sector's prices can be refreshed on demand; scheduled runs unchanged; generate-signals dropdown also updated to include Crypto (`.github/workflows/update-prices.yml`, `.github/workflows/generate-signals.yml`). |
| 2026-06-01 | Claude Sonnet 4.6 (Claude Code) | Web | **Crypto sector launched** — 16 stocks across 5 categories (Bitcoin ETFs: IBIT/FBTC/ARKB/GBTC/BITO; Ethereum ETFs: ETHA/FETH; exchanges: COIN/HOOD; treasury: MSTR; miners: MARA/RIOT/CLSK/HUT/IREN/CIFR); full 7-page suite; `Crypto_update_prices.py`, `Crypto_generate_signals_local.py`, `requirements.txt`; GitHub Actions workflows updated; bat files updated to [5/5]; Crypto sector card added to all 28 existing sector pages; hub `index.html` updated to live card + SECTORS array (`Crypto/`, `.github/workflows/update-prices.yml`, `.github/workflows/generate-signals.yml`, `update_all_prices.bat`, `generate_all_signals.bat`, `index.html`, `FEATURES.md`, `CHANGELOG.md`). |
| 2026-06-01 | Claude Sonnet 4.6 (Claude Code) | Web | Hub: **picks redesigned as metrics-style table** — full-bleed table per sector card (bleeds to card edges via negative margin); columns: `#` / Company (ticker + name + SB/B badge) / 1D / 1W / YTD / Conf; colour-coded returns; row hover; company name hidden on mobile; clicking table → signals page, clicking card header → dashboard. `index.html` |
| 2026-06-01 | Claude Sonnet 4.6 (Claude Code) | Web | Fix: **mobile light mode background** — added `html { background: var(--bg) }` to `shared.css` and `index.html`; fixes iOS Safari overscroll area and browser chrome showing grey instead of white. |
| 2026-06-01 | Claude Sonnet 4.6 (Claude Code) | Web | Hub: **1D / 1W / YTD on picks** — each pick fetches price performance from `prices-data.js` alongside `signals-local.json`; colour-coded second line per pick (green = positive, accent2 = negative). `index.html` |
| 2026-06-01 | Claude Sonnet 4.6 (Claude Code) | Web | Hub: **signals section click target** — removed "→ all signals" link; clicking the `// Top Signals` block navigates to that sector's signals page; label turns accent on hover. `index.html` |
| 2026-06-01 | Claude Sonnet 4.6 (Claude Code) | Web | Hub: **card alignment fix** — grid changed to fixed 2-column layout; picks block wrapped in `flex: 1` container; responsive 1-col breakpoint at ≤640px. `index.html` |
| 2026-06-01 | Claude Sonnet 4.6 (Claude Code) | Web | Hub: **top 5 signals picks** — each sector card shows top 5 AI signal picks fetched live from `signals-local.json`; confidence bar + signal badge per pick. `index.html` |
| 2026-06-01 | Claude Sonnet 4.6 (Claude Code) | Desktop | Infra: **branch cleanup + dev workflow established** — deleted stale `cloudflare/workers-autoconfig` and `claude/magical-davinci-zYLYI` remote branches; `dev` branch synced to `main`; confirmed workflow: all changes pushed to `dev` → auto-deploys to `https://dev.stocks-4qw.pages.dev` for remote preview → `main` only updated on explicit "go". |
| 2026-06-01 | Claude Sonnet 4.6 (Claude Code) | Web | UX: **disclaimer consolidation** — all page-specific `.disclaimer` divs removed from `metrics.html`, `news.html`, `calculator.html`, and `signals.html` across all 4 sectors (16 pages); page-specific caveats (data delays, VADER scoring, YTD methodology, AI-generated signals) folded into the `header-note` at the top of each page; no duplicate warnings anywhere. |
| 2026-06-01 | Claude Sonnet 4.6 (Claude Code) | Web | Infra: **sector selector on generate-signals workflow** — `workflow_dispatch` now has a dropdown (all / AI / Biotech / Defence / Tech) so a single sector can be refreshed; scheduled runs unchanged (`generate-signals.yml`). |
| 2026-06-01 | Claude Sonnet 4.6 (Claude Code) | Web | Infra: **sector-prefixed script names** — `update_prices.py` → `[Sector]_update_prices.py`; `generate_signals_local.py` → `[Sector]_generate_signals_local.py` across all 4 sectors; workflows, bat files, and all docs updated. |
| 2026-06-01 | Claude Sonnet 4.6 (Claude Code) | Web | Fix: **bot push-rejection** — added `git pull --rebase` before `git push` in both `update-prices.yml` and `generate-signals.yml`; prevents failed pushes when commits land on main while a bot job is running. |
| 2026-06-01 | Claude Sonnet 4.6 (Claude Code) | Web | **Technology sector launched** — 31 stocks across 6 categories (mega-cap, semiconductors, enterprise software, cybersecurity, consumer platforms, hardware/infra); full 7-page suite (`index.html`, `metrics.html`, `news.html`, `signals.html`, `heatmap.html`, `charts.html`, `calculator.html`); `update_prices.py`, `generate_signals_local.py`, `requirements.txt`; GitHub Actions workflows updated; bat files updated to [4/4]; Tech sector card added to all 21 existing pages; hub `index.html` updated (`Tech/`, `index.html`, `.github/workflows/update-prices.yml`, `.github/workflows/generate-signals.yml`, `update_all_prices.bat`, `generate_all_signals.bat`, `AGENTS.md`, `FEATURES.md`, `CHANGELOG.md`). |
| 2026-05-31 | Claude Sonnet 4.6 (Claude Code) | Desktop | Infra: **migrated hosting from Netlify to Cloudflare Pages** — live site now at https://stocks-4qw.pages.dev; `dev` branch auto-deploys as preview; `WORKFLOW.md` and `AGENTS.md` updated to remove Netlify references (`WORKFLOW.md`, `AGENTS.md`). |
| 2026-05-31 | Claude Sonnet 4.6 (Claude Code) | Desktop | Fix: **COAG YTD data gap** — switched YTD fetch from fixed date-range (`start/end`) to `period='ytd'` so the first real trading day of the year is used regardless of data gaps; added `contextlib.redirect_stderr` to suppress yfinance's misleading "possibly delisted" stderr noise; COAG now shows -16.18% YTD instead of +0.00% (`AI/update_prices.py`, `Biotech/update_prices.py`, `Defence/update_prices.py`). |
| 2026-05-31 | Claude Sonnet 4.6 (Claude Code) | Desktop | Docs: **log filenames corrected in MDs** — `CHANGELOG.md` and `FEATURES.md` referenced `signals_log.txt` and `update_log.txt`; actual filenames are `generate_all_signals_log.txt` and `update_all_prices_log.txt` (`CHANGELOG.md`, `FEATURES.md`). |
| 2026-05-31 | Claude Sonnet 4.6 (Claude Code) | Desktop | UX: **charts toolbar restructured** — category filter row now sits above view/period controls; `.cat-btn` styled to metrics filter-btn spec (10px / 2px / 5px 12px); `toolbar-inner` is now column-flex with `toolbar-row` sub-rows; category row hides in single view, shows in grid view (`AI/charts.html`, `Biotech/charts.html`, `Defence/charts.html`). |
| 2026-05-31 | Claude Sonnet 4.6 (Claude Code) | Desktop | Style: **header labels and h1s standardised** — all 18 sub-pages now follow the index.html pattern: `// Stock Universe` label + sector-prefixed h1 (e.g. "AI Metrics", "Biotech News", "Defence Charts") replacing generic "// AI Infrastructure", "Stock Metrics", "Market Heatmap", "Price Charts", "What-If Calculator" etc (all sector pages except index). |
| 2026-05-31 | Claude Sonnet 4.6 (Claude Code) | Desktop | Style: **icons removed from category filter buttons** — all filter/category buttons on index + charts pages (all 3 sectors) now use plain text labels matching the metrics standard; label text also aligned (e.g. "Memory / Storage" → "Memory", "Nuclear Operators" → "Nuclear", "Test Equipment" → "Test Equip") (`AI/index.html`, `AI/charts.html`, `Biotech/index.html`, `Biotech/charts.html`, `Defence/index.html`, `Defence/charts.html`). |
| 2026-05-31 | Claude Sonnet 4.6 (Claude Code) | Desktop | Style: **filter buttons fully standardised** — all `.filter-btn` across index + news pages (all 3 sectors) now match metrics spec: `letter-spacing: 2px`, `transition: 0.15s`, hover = accent colour only (no bg fill), active = full fill + `font-weight: 700`, tooltip bg = `var(--surface2)`, `.filters` → `.filter-bar` with `gap: 6px` / `margin-bottom: 16px` (`AI/index.html`, `Biotech/index.html`, `Defence/index.html`, `AI/news.html`, `Biotech/news.html`, `Defence/news.html`). |
| 2026-05-31 | Claude Sonnet 4.6 (Claude Code) | Desktop | Fix: **charts toolbar alignment** — toolbar content was misaligned with nav/blue line; `toolbar-inner` padding corrected to `0 24px` to match `nav-inner`; `z-index` raised from 5→25 so category tooltips render above page content (`AI/charts.html`, `Biotech/charts.html`, `Defence/charts.html`). |
| 2026-05-31 | Claude Sonnet 4.6 (Claude Code) | Desktop | UX: **category filter tooltips on metrics + charts** — `CAT_TOOLTIPS` dict added to all 3 metrics pages; `tooltip` field added to `CHART_CATS` entries in all 3 charts pages; tooltip CSS (`::after/::before`) added to both page types; all filter/category buttons now have hover popups across all 6 pages. |
| 2026-05-31 | Claude Sonnet 4.6 (Claude Code) | Desktop | UX: **financial advice disclaimer moved to header** — `.header-note` strip (bordered, full width, below sector switcher) added to all 21 sector pages via `shared.css`; old `<footer>` disclaimer blocks removed from all sector pages; hub `index.html` footer unchanged (`shared.css`, all 21 sector pages). |
| 2026-05-31 | Claude Sonnet 4.6 (Claude Code) | Desktop | UX: **What-If disclaimer moved to bottom** — disclaimer moved from top of content to below the table (inside `.container`) on all 3 sectors (`AI/calculator.html`, `Biotech/calculator.html`, `Defence/calculator.html`). |
| 2026-05-31 | Claude Sonnet 4.6 (Claude Code) | Desktop | Docs: **sector AGENTS.md filter-btn spec standardised** — `Biotech/AGENTS.md` and `Defence/AGENTS.md` updated from 12px/8px 16px to 10px/5px 12px to match live standard. |
| 2026-05-31 | Claude Sonnet 4.6 (Claude Code) | Desktop | Refactor: **dashboard + charts fully dynamic** — `index.html` (all 3 sectors) now builds the stock table from `prices-data.js` via `buildTable()`; `charts.html` (all 3 sectors) builds sidebar buttons from `prices-data.js` via `buildSidebar()`; `update_prices.py` (all 3 sectors) adds `company_name` field to output and removes BeautifulSoup HTML-patching. Adding a stock now requires only: STOCKS entry in `update_prices.py`, note in `index.html` NOTES dict, and TV_MAP entry in `charts.html` if TradingView symbol differs (`AI/`, `Biotech/`, `Defence/` — `update_prices.py`, `index.html`, `charts.html`). |
| 2026-05-31 | Claude Sonnet 4.6 (Claude Code) | Desktop | Fix: **hardcoded stock counts** — `visible-count` defaults updated to use dynamic `—` placeholder; `applyFilters()` called on `DOMContentLoaded` so counts always reflect actual data; Biotech default fixed from 29 → 30 (`AI/index.html`, `Biotech/index.html`, `Defence/index.html`). |
| 2026-05-31 | Claude Sonnet 4.6 (Claude Code) | Desktop | Fix: **PSTG → P (Everpure)** — heatmap `NAMES` lookup updated (`'PSTG': 'Pure Storage'` → `'P': 'Everpure'`); charts `NO_EMBED` and button updated from `NYSE:PSTG` → `NYSE:P` (`AI/heatmap.html`, `AI/charts.html`). |
| 2026-05-31 | Claude Sonnet 4.6 (Claude Code) | Desktop | UX: **disclaimers moved to bottom** — "For research only" disclaimer now appears below content (above footer) on all news and signals pages, all 3 sectors (`AI/news.html`, `Biotech/news.html`, `Defence/news.html`, `AI/signals.html`, `Biotech/signals.html`, `Defence/signals.html`). |
| 2026-05-31 | Claude Sonnet 4.6 (Claude Code) | Desktop | UX: **filter buttons standardised** — all filter buttons across all pages set to `font-size: 10px`, `padding: 5px 12px` to match news pages (all `index.html` and `metrics.html` files). |
| 2026-05-30 | Claude Sonnet 4.6 (Claude Code) | Desktop | UX: **heatmap metric persists across sector switches** — selected timeframe (1D/1W/1M/YTD/1Y) saved to `localStorage` and restored on page load; switching sectors via the sector switcher now keeps the active metric (`AI/heatmap.html`, `Biotech/heatmap.html`, `Defence/heatmap.html`). |
| 2026-05-30 | Claude Sonnet 4.6 (Claude Code) | Desktop | Fix: **heatmap 1Y view blank** — field key was `return_1y` in the heatmap JS but `return_1yr` in `prices-data.js`; all 3 sectors affected (`AI/heatmap.html`, `Biotech/heatmap.html`, `Defence/heatmap.html`). |
| 2026-05-30 | Claude Sonnet 4.6 (Claude Code) | Desktop | UX: **metrics filter-btn padding/spacing** — padding `5px 12px → 8px 20px`, letter-spacing `1px → 2px` to match the nav bar on all 3 sectors (`AI/metrics.html`, `Biotech/metrics.html`, `Defence/metrics.html`). |
| 2026-05-30 | Claude Sonnet 4.6 (Claude Code) | Web | Fix: **signals API timeout** — changed from single 180s value to tuple `(15s connect, 120s read)` so hung TCP connections (provider accepts but never responds) now time out correctly; `python -u` flag added to both bat files for unbuffered live output (`AI/generate_signals_local.py`, `Biotech/generate_signals_local.py`, `Defence/generate_signals_local.py`, `update_all_prices.bat`, `generate_all_signals.bat`). |
| 2026-05-30 | Claude Sonnet 4.6 (Claude Code) | Web | **`generate_all_signals.bat`** — root-level script runs all 3 sector signal generators in sequence; output streams live and saves to `generate_all_signals_log.txt` (overwritten each run) (`generate_all_signals.bat`). |
| 2026-05-30 | Claude Sonnet 4.6 (Claude Code) | Web | Fix: **TradingView range `'1W'` → `'5D'`** — `'1W'` is not a valid TradingView widget range; caused "Something went wrong" on all charts grids; fixed in all 3 sectors (`AI/charts.html`, `Biotech/charts.html`, `Defence/charts.html`). |
| 2026-05-30 | Claude Sonnet 4.6 (Claude Code) | Web | **Charts default to Category Grid / 1W / first category** — all 3 charts pages now open on grid view, 1-week timeframe, first category pre-selected (AI=Memory, Biotech=Large Cap, Defence=US Primes) (`AI/charts.html`, `Biotech/charts.html`, `Defence/charts.html`). |
| 2026-05-30 | Claude Sonnet 4.6 (Claude Code) | Web | Fix: **missing `</script>` tags** across 14 pages — agent that removed `buildTape()` stripped closing script tags from metrics×3, news×3, charts×3, calculator×3, index×2; caused blank pages on all affected views. |
| 2026-05-30 | Claude Sonnet 4.6 (Claude Code) | Web | **PSTG → P** — Pure Storage rebranded to Everpure (NYSE: P, Apr 2026); updated ticker in `AI/update_prices.py`, `AI/index.html`, `AI/metrics.html`, and `prices-data.js`. |
| 2026-05-30 | Claude Sonnet 4.6 (Claude Code) | Web | **`update_all_prices.bat`** — root-level batch file runs all 3 sector price updates in sequence; output logged to `update_all_prices_log.txt` (overwritten each run) via PowerShell `Tee-Object` so log is readable without copy-pasting (`update_all_prices.bat`). |
| 2026-05-30 | Claude Sonnet 4.6 (Claude Code) | Web | Fix: **COAG missing from Biotech dashboard** — Hemab Therapeutics was in the ticker list and `prices-data.js` but had no table row; added to Rare Disease section (`Biotech/index.html`). Now correctly shows 30 of 30 stocks. |
| 2026-05-30 | Claude Sonnet 4.6 (Claude Code) | Web | Fix: **Biotech media query 900px → 768px** — `Biotech/index.html` was the only file using a 900px breakpoint; at split-screen widths fonts shrank while AI/Defence stayed full-size (`Biotech/index.html`). |
| 2026-05-30 | Claude Sonnet 4.6 (Claude Code) | Web | **Dashboard headers** — sector name now the large h1 ("AI Infrastructure", "Biotech", "Defence & Aerospace"); "Stock Universe" moved to small monospace label above; Biotech h1 split: "Bio" dark / "tech" blue accent (all 3 `index.html` files). |
| 2026-05-30 | Claude Sonnet 4.6 (Claude Code) | Web | **Best-practice refactor** — `shared.js` created (pulls `toggleTheme()` + `buildTape()` out of 22 inline copies); `.disclaimer` CSS moved to `shared.css`; `prices-data.js` moved from `<head>` to `<body>`; `<meta name="description">` added to all 22 pages; −992 lines of duplicate code (`shared.js`, `shared.css`, all HTML files). |
| 2026-05-29 | Claude Sonnet 4.6 (Claude Code) | Web | **Light/dark mode toggle — full rollout** — ☾/☀ button in header sector-switcher on all 22 pages (root hub + AI×7 + Biotech×7 + Defence×7); light mode default (`:root`); dark mode via `html[data-theme="dark"]`; `localStorage` persistence; flash-prevention script in every `<head>`; heatmap D3 colours driven by CSS vars (`getCssVar()`) with re-render on toggle; `--muted`/`--slate` darkened in light mode; hardcoded colour fixes throughout (`shared.css`, `index.html`, all sector pages). |
| 2026-05-28 21:56 | Claude Sonnet 4.6 (Claude Code) | Web | **Biotech filter buttons standardised** — padding, letter-spacing, text-transform, font-weight, transition now match AI and Defence exactly; responsive override added (`Biotech/index.html`). |
| 2026-05-28 21:23 | Claude Sonnet 4.6 (Claude Code) | Web | **Sector switcher replaces back-link** across all 21 sector pages — mini cards for all 3 sectors in header right side; active sector bright with cyan top border, others dim; links are context-aware (stay on same page type when switching); Stock Hub card links back to root `index.html`; `shared.css` updated with `.sector-switcher`, `.sector-card`, `.hub-card` styles; no `.back-link` HTML or CSS remains (`AI/*.html`, `Biotech/*.html`, `Defence/*.html`, `shared.css`). |
| 2026-05-28 20:35 | Claude Sonnet 4.6 (Claude Code) | Web | **shared.css** — extracted all shared CSS (reset, body, ticker tape, header, nav, footer, container) from all 21 sector pages into a single `shared.css`; Google Fonts loaded via `<link>` tag (not `@import` — needed for `file://` compatibility); change pills and notes-text unified to single spec across all dashboards (`shared.css`, all 21 sector page `<style>` blocks). |
| 2026-05-28 18:41 | Claude Sonnet 4.6 (Claude Code) | Web | **Defence & Aerospace sector launched** — 28 stocks across 6 categories (US Primes, UK/European, Cyber/Intel, Drones, Space, Weapons/Systems); full suite of 7 pages (dashboard, metrics, news, signals, heatmap, charts, calculator); prices via GitHub Actions; signals via OpenRouter DeepSeek; LSE stocks (BA.L, RR.L, QQ.L) divided by 100 for GBP (`Defence/`, `index.html`, `.github/workflows/`). |
| 2026-05-27 23:29 | Claude Sonnet 4.6 (Claude Code) | Web | **Rank delta badges** on AI and Biotech signals pages — show movement since last run (▲↑↓▼ with colour); signals schedule updated to Mon/Wed/Fri; "How It Works" section added to signals pages (`AI/signals.html`, `Biotech/signals.html`). |
| 2026-05-27 22:31 | Claude Sonnet 4.6 (Claude Code) | Web | **Web search plugin** added to AI and Biotech signals generators — DeepSeek can pull live news during signal generation via OpenRouter web search tool (`AI/generate_signals_local.py`, `Biotech/generate_signals_local.py`). |
| 2026-05-27 22:44 | Claude Sonnet 4.6 (Claude Code) | Web | Fix: **filter button tooltips** now appear above buttons (not clipped below nav) on all sector dashboards — `bottom: calc(100% + 10px)` positioning (`AI/index.html`, `Biotech/index.html`). |
| 2026-05-27 20:49 | Claude Sonnet 4.6 (Claude Code) | Web | **Category filter tooltips** on all sector news pages — hover on category filter buttons to see descriptions; **nav link tooltips** added to Biotech dashboard (`AI/news.html`, `Biotech/news.html`, `Biotech/index.html`). |
| 2026-05-26 01:55 | Claude Sonnet 4.6 (Claude Code) | Web | **All dashboard columns sortable** — 52-Week Range and Key Angle columns now sortable on both AI and Biotech dashboards (`AI/index.html`, `Biotech/index.html`). |
| 2026-05-26 01:54 | Claude Sonnet 4.6 (Claude Code) | Web | Fix: **Company column sortable** on Biotech and AI metrics tables (`AI/metrics.html`, `Biotech/metrics.html`). |
| 2026-05-26 01:52 | Claude Sonnet 4.6 (Claude Code) | Web | Fix: stale tooltip in Biotech nav — replaced 'AI top 10 picks' with 'Top 10 biotech picks' (`Biotech/signals.html`, `Biotech/news.html`, `Biotech/heatmap.html`, `Biotech/charts.html`, `Biotech/calculator.html`, `Biotech/metrics.html`). |
| 2026-05-26 00:42 | Claude Sonnet 4.6 (Claude Code) | Web | Fix: Biotech signals page h1 read 'AI Signals' → corrected to 'Biotech Signals' (`Biotech/signals.html`). |
| 2026-05-25 13:45 | Claude Sonnet 4.6 (Claude Code) | Web | Added **COAG** (Hemab Therapeutics) to Biotech sector — 30 stocks total (`Biotech/update_prices.py`, `Biotech/index.html`, `Biotech/news.html`). |
| 2026-05-24 21:36 | Claude Sonnet 4.6 (Claude Code) | Web | Built **Biotech sector dashboard** — 29 stocks across 9 categories (large-cap, UK listed, gene editing, genomics, oncology, mRNA, rare disease, metabolic, neuroscience); full suite: metrics, news, signals, heatmap, charts, calculator; prices via GitHub Actions; `Biotech/` subfolder + workflow updated (`Biotech/`, `.github/workflows/update-prices.yml`, `.github/workflows/generate-signals.yml`, `index.html`, `FEATURES.md`). |
| 2026-05-24 16:41 | Claude Sonnet 4.6 (Claude Code) | Web | Heatmap: added prices **last-updated timestamp** to toolbar (`AI/heatmap.html`). |
| 2026-05-24 | Claude Sonnet 4.6 (Claude Code) | Web | **Full migration from KobiLDN/aiSTOCKS** — copied all AI sector files into `AI/`; moved `.github/workflows/` to repo root with `working-directory: AI` on all steps; built hub landing page (`index.html`) with sector cards; added `← All Sectors` back link to all 7 AI pages; added root `.gitignore`; repo docs created (`AGENTS.md`, `WORKFLOW.md`, `CHANGELOG.md`, `FEATURES.md`). v1. |
