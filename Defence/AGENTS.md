# Defence & Aerospace Sector — Agent Guide

Read the **root `../AGENTS.md` first** — it contains the full workflow, git rules, architecture decisions, UI design standards, and sector switcher structure. This file covers Defence-specific details only.

## Structure

29 stocks across 6 categories. All prices in GBP.

| Category | Slug | Colour token | Tickers |
|---|---|---|---|
| US Primes | `us-primes` | `--accent` (cyan) | LMT, RTX, NOC, GD, HII, LHX, BA |
| UK/European | `uk-listed` | `--gold` | BA.L, RR.L, QQ.L |
| Cyber/Intel | `cyber-intel` | `--purple` | PLTR, LDOS, CACI, SAIC, BAH |
| Drones | `drones` | `--green` | AVAV, KTOS, RCAT, TXT |
| Space | `space` | `--teal` | SPCX, RKLB, PL, ASTS |
| Weapons/Systems | `weapons` | `--rose` | TDG, HEI, AXON, OLN, MRCY, DRS |

## UK/European stocks (BA.L, RR.L, QQ.L)

- Yahoo Finance symbols: `BA.L`, `RR.L`, `QQ.L`
- yfinance returns prices in **GBp (pence)** — divide by 100 to get £
- TradingView widget symbols: `LSE:BA`, `LSE:RR`, `LSE:QQ`
- Note: `BA.L` is BAE Systems (UK), not to be confused with Boeing (`BA` on NYSE)

## Pages

| Page | File | Description |
|---|---|---|
| Dashboard | `index.html` | Live prices, 52-week range, momentum pills for all 29 stocks |
| Metrics | `metrics.html` | Market cap, P/E, beta, dividend yield, short interest, analyst ratings |
| News | `news.html` | Headlines across all 29 stocks with VADER sentiment scoring |
| Signals | `signals.html` | Top 10 ranked picks — generated by DeepSeek via OpenRouter |
| Heatmap | `heatmap.html` | D3 treemap — tile size = \|% move\|, colour = direction |
| Charts | `charts.html` | TradingView embeds, single-stock and category grid views |
| What-If | `calculator.html` | Jan 2026 investment simulator |

## Shared stylesheet

All Defence pages load `../shared.css` for shared styles (reset, body, ticker tape, header, nav, footer, sector switcher). Page-specific CSS lives in each page's `<style>` block only. Do not duplicate shared rules.

## UI standards — follow AI sector exactly

`AI/index.html` is the canonical reference. Defence must match all font sizes and layout tokens. See the **UI Design Standards** table in `../AGENTS.md`.

| Element | Size | Notes |
|---|---|---|
| `h1` | 36px | 26px at ≤900px breakpoint |
| `.header-sub` | 13px | |
| `.nav-link` | 12px | |
| `.filter-btn` | 10px | `padding: 5px 12px` |
| `th` | 11px | |
| `td` | 14px | |
| `.ticker` | 15px | |
| `.price` | 17px | |

## Automated updates

- **Prices** — GitHub Actions three times daily on weekdays (08:00, 14:30, 20:30 UTC) via `yfinance`. Writes `index.html`, `prices.json`, `prices-data.js`.
- **Signals** — GitHub Actions weekly (Mondays 07:00 UTC) via OpenRouter (`deepseek/deepseek-v4-flash`). Writes `signals-local.json`, `signals-local.js`. Uses `OPENROUTER_API_KEY` repository secret.

## Local development

```
cd Defence
python Defence_update_prices.py  # refresh prices
python Defence_generate_signals_local.py # refresh signals (requires key.txt)
```

`key.txt` (your OpenRouter key) is gitignored — never commit it.

## Defence-specific signal prompt context

The signals generator includes defence-sector-specific instructions: defence budget cycles, contract awards and programme milestones, geopolitical threat environment, export restrictions (ITAR/EAR), backlog-to-revenue ratios, and competitive dynamics across primes and disruptors.
