# Features

Backlog of planned work and ideas. AI should read this before making changes to understand what is coming.

---

## Backlog

_(no open items)_

---

## Done

- **Default sort by 1yr Return (high → low)** — table loads pre-sorted on page open (`index.html`).
- **1D / 1W / 1M change pills** — coloured pills under each price showing daily, weekly, and monthly % change; populated by price bot (`update_prices.py`, `index.html`).
- **YTD sortable column** — year-to-date % change as its own column beside 1yr Return; colour-coded green/red; sortable; Risk and Revenue columns removed (`index.html`, `update_prices.py`).
- **Ticker search bar** — real-time text filter by ticker or company name, inline with stock count; works alongside category filter buttons (`index.html`).
- **What-If calculator page** — Jan 2026 investment simulation showing current value of £100/£1k/£10k per stock based on YTD return; sortable columns; totals row; search box (`calculator.html`).
- **Stock metrics page** — dedicated page showing market cap, beta, P/E, avg vol, 1D/1W/1M volume, YTD, dividend yield, short interest, and analyst rating per stock; sortable; category filter; search; tooltips on column headers (`metrics.html`, `update_prices.py`).
- **Yahoo Finance ticker links** — ticker symbols on dashboard and calculator link to Yahoo Finance; correct suffixes for RR. (RR.L) and 285A (285A.T) (`index.html`, `calculator.html`).
- **v2.0 layout overhaul** — header above nav on all pages; nav below header with full-width separator; blue accent line as `::before` pseudo locked to the same `left:24px` rail as the Dashboard/All button borders; all pages standardised to `max-width: 1600px` so the header no longer jumps when navigating; `header-inner`/`nav-inner` alignment wrappers (`index.html`, `charts.html`, `metrics.html`, `calculator.html`).
- **News page** — dedicated `news.html` feed of latest headlines across all 48 stocks; keyless pipeline (yfinance `.news` + local VADER sentiment); sentiment-coloured cards with ticker (Yahoo link), company, category badge, time-ago, publisher; sort by newest/oldest/most positive/most negative; category filter; search; News added to nav after Charts on all pages (`news.html`, `update_prices.py`, `requirements.txt`, `index.html`, `metrics.html`, `charts.html`, `calculator.html`).
- **Heatmap page** — dedicated `heatmap.html`; D3.js `d3.treemap()`; tile size = `|% change|` for selected metric (bigger move = bigger tile); tile colour = TradingView-palette diverging scale (`#ef5350` coral red → dark navy → `#4caf50` emerald green); metric switcher (1D / 1W / 1M / YTD / 1Y) sticky at top; layout: **All Stocks** flat treemap (~58vh) + **By Sector** (12 full-width treemaps stacked, each category gets own canvas — no squashing); text scales dynamically with tile size (IBM Plex Sans regular, formula-driven); black pill bg behind each label for legibility; hover tooltip (all metrics + price + market cap); click → Yahoo Finance; responsive; no new bot fields (`heatmap.html`, nav on all 7 pages).
- **AI Signals page** — dedicated `signals.html` showing 10 ranked picks with rank badge, ticker (Yahoo link), company, category, signal pill (STRONG BUY / BUY), confidence bar, rationale, and driver chips. Generated by a **local** LLM (Qwen 3.5 9B reasoning-distilled in LM Studio) via `generate_signals_local.py`. Keyless (no API keys, no GitHub secrets — superseding the earlier heuristic-only plan in favour of a local LLM that respects the same no-secrets rule). Pipeline is LOCAL-ONLY: cannot run in CI, refreshed manually by the user when they want fresh picks. Output committed as `signals-local.json` + `signals-local.js` so the live page works immediately on push (`signals.html`, `generate_signals_local.py`, `signals-local.json`, `signals-local.js`, nav on all 6 pages).
