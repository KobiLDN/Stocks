# Changelog

Columns: `Date · time (BST)`, `AI Name (Tool)`, `Where` (Desktop / Mobile — which Claude client the change was made from), `Changed` (short description with primary file(s) in backticks).

All notable changes to this repository are documented here — **newest first**. Use **BST** for the changelog so it stays aligned with `index.html` last-updated timings.

| Date · time (BST) | AI Name | Where | Changed |
|---|---|---|---|
| 2026-05-23 23:30 | Claude Opus 4.7 (Claude Code) | Desktop | **OpenRouter/DeepSeek CI integration** — switched AI Signals from local LM Studio to `deepseek/deepseek-v4-flash` via OpenRouter (~$0.004/run); `generate_signals_local.py` rewritten to use `OPENROUTER_API_KEY` env var (CI) or `key.txt` (local), `response_format: json_object`, 180s timeout; new `.github/workflows/generate-signals.yml` runs weekly (Mondays 07:00 UTC) + `workflow_dispatch`; `signals.html` empty-state + refresh banner updated; `AGENTS.md` keyless rule updated to document the approved exception; bumped to v2.4 (`generate_signals_local.py`, `.github/workflows/generate-signals.yml`, `signals.html`, `AGENTS.md`). |
| 2026-05-23 22:00 | Claude Opus 4.7 (Claude Code) | Desktop | Heatmap text legibility — replaced black pill background with **stroke outline** (`stroke-width: 4, paint-order: stroke`) on ticker and value labels; pill CSS and JS drawing code removed; cleaner look matching TradingView style more closely (`heatmap.html`). |
| 2026-05-23 21:30 | Claude Opus 4.7 (Claude Code) | Desktop | Heatmap polish — tile text now **regular weight IBM Plex Sans** (not bold monospace) matching TradingView style; **dynamic font sizing** scales continuously with tile area (formula: `min(tileW×0.60 / chars×0.62, tileH×0.30, 30px)` for ticker, `18px` cap for value); colours updated to TradingView palette (`#ef5350` coral red · `#4caf50` emerald green); **black pill** (`rgba(0,0,0,0.68)`, rx=2) behind each label for legibility on any tile colour; legend gradient updated to match (`heatmap.html`). |
| 2026-05-23 20:00 | Claude Opus 4.7 (Claude Code) | Desktop | Heatmap redesign — tile size now = **\|% change\|** (bigger move = bigger tile, not market cap); layout split into **All Stocks** (one big flat treemap, ~58vh) + **By Sector** (12 separate treemaps stacked below, each getting full width so small categories like Materials/Test Equip are no longer squashed); toolbar sticky so it stays visible while scrolling; sector maps sized proportionally to stock count (`heatmap.html`). |
| 2026-05-23 19:30 | Claude Opus 4.7 (Claude Code) | Desktop | Built the **Heatmap page** — new `heatmap.html` treemap of all 48 stocks via D3.js `d3.treemap()`; tile size = market cap; tile colour = performance metric (red → dark navy → green diverging scale); metric switcher (1D / 1W / 1M / YTD / 1Y) with smooth colour transition; tiles grouped by category with uppercase labels in paddingTop area; hover tooltip shows all 5 metrics + price + market cap; click tile → Yahoo Finance; responsive (re-renders on resize); legend updates threshold per metric; 🔥 Heatmap added to nav between Signals and Charts on all 7 pages; bumped to v2.3 (`heatmap.html`, `index.html`, `metrics.html`, `news.html`, `signals.html`, `charts.html`, `calculator.html`, `FEATURES.md`). |
| 2026-05-23 18:00 | Claude Opus 4.7 (Claude Code) | Desktop | Added a **Where** column to the CHANGELOG (Desktop / Mobile — which Claude client the change was made from); all 32 prior entries backfilled as `Desktop`; convention documented in `AGENTS.md` "After every edit" section (`CHANGELOG.md`, `AGENTS.md`). |
| 2026-05-23 17:30 | Claude Opus 4.7 (Claude Code) | Desktop | Built the **AI Signals** page — new `signals.html` with 10 ranked picks rendered as cards (rank, ticker/Yahoo link, company, category badge, signal pill, confidence bar, rationale, driver chips); category comes from `prices-data.js` cross-reference; empty state with refresh instructions when no data; Signals added to nav between News and Charts on all 6 pages. Picks come from `generate_signals_local.py` — a **local-only** pipeline that POSTs a compact 48-stock snapshot to LM Studio (`localhost:1234`) running `qwen3.5-9b-claude-4.6-opus-reasoning-distilled-v2`, strips `<think>` traces, validates output. Strict `json_schema` mode disabled (was strangling the reasoning model). First run committed alongside (`signals.html`, `generate_signals_local.py`, `signals-local.json`, `signals-local.js`, `index.html`, `metrics.html`, `news.html`, `charts.html`, `calculator.html`, `FEATURES.md`). |
| 2026-05-19 14:45 | Claude Opus 4.7 (Claude Code) | Desktop | Metrics page: reordered columns to Price · Market Cap · YTD · (rest) — Price now first of the numeric columns (`metrics.html`, `CHANGELOG.md`). |
| 2026-05-19 14:30 | Claude Opus 4.7 (Claude Code) | Desktop | Metrics page: added a sortable **Price** column (GBP) immediately left of Market Cap, with header tooltip; data mapped from `price_gbp`; empty-state colspan fixed to 15 (`metrics.html`, `CHANGELOG.md`). |
| 2026-05-19 14:00 | Claude Opus 4.7 (Claude Code) | Desktop | News page: added a **Period filter** (All Time / Last 24h / Last Week) that stacks with sort + category + search — e.g. "Most Positive" + "Last 24h" = most positive headlines in the last day; scoped sort/period button groups independently (`news.html`, `CHANGELOG.md`). |
| 2026-05-19 13:30 | Claude Opus 4.7 (Claude Code) | Desktop | Built the **News page** — new `news.html` headline feed (keyless: yfinance `.news` + local VADER sentiment); `get_news()` added to `update_prices.py` (guarded VADER import, defensive about yfinance's two news shapes, never breaks price updates) writing `news`/`news_sentiment` into `prices.json`/`prices-data.js`; `vaderSentiment` added to `requirements.txt`; sentiment-coloured cards, sort (newest/oldest/+/−), category filter, search; News nav link added after Charts on all pages; bumped to v2.1 (`news.html`, `update_prices.py`, `requirements.txt`, `index.html`, `metrics.html`, `charts.html`, `calculator.html`, `FEATURES.md`). |
| 2026-05-19 12:30 | Claude Opus 4.7 (Claude Code) | Desktop | Refined News feature spec — decided on a **dedicated `news.html` page** (not a dashboard column), mirroring `metrics.html`; updated the `FEATURES.md` entry with full design notes (`FEATURES.md`, `CHANGELOG.md`). |
| 2026-05-19 12:00 | Claude Opus 4.7 (Claude Code) | Desktop | Locked the **keyless-only** architectural decision — no API keys/secrets ever; AI Top 10 Picks → heuristic ranking, News sentiment → yfinance `.news` + local VADER; recorded in `AGENTS.md` (new hard rule) and pruned both `FEATURES.md` backlog entries to the chosen approach (`AGENTS.md`, `FEATURES.md`, `CHANGELOG.md`). |
| 2026-05-19 00:00 | Claude Opus 4.7 (Claude Code) | Desktop | Added `WORKFLOW.md` — documents the two-clone structure, 3-step DEV→MAIN→GitHub workflow, safety net, branch hygiene, and a reusable setup prompt for future repos; added it to the AGENTS.md read order (`WORKFLOW.md`, `AGENTS.md`, `CHANGELOG.md`). |
| 2026-05-18 23:30 | Claude Opus 4.7 (Claude Code) | Desktop | Restructured local repo to two independent clones in an `aiStocks\` container (`aiStocksMAIN` / `aiStocksDEV`) — no more git worktrees; documented the 3-step DEV→MAIN→GitHub workflow, the restore-from-main safety net, and the ff-only/rebase fallback in `AGENTS.md`; added Local development section to `README.md`; fixed stale `server.py`/`start.bat` note (`AGENTS.md`, `README.md`, `CHANGELOG.md`). |
| 2026-05-18 22:00 | Claude Opus 4.7 (Claude Code) | Desktop | Fact-checked and corrected `README.md` — stock count 49→48; added Metrics & What-If pages; bot section now lists all 3 output files + metrics fetch; Files table adds `metrics.html`/`calculator.html`, fixes `update_prices.py` desc and `tzdata` dependency; added runtime-data freshness note (`README.md`). |
| 2026-05-17 02:30 | Claude Opus 4.7 (Claude Code) | Desktop | Documented `prices-data.js` deployment gap in `AGENTS.md` — new bot output fields don't appear live until the GitHub workflow regenerates the file; added rule to commit a fresh `prices-data.js` (or run the workflow) when adding fields (`AGENTS.md`, `CHANGELOG.md`, `FEATURES.md`). |
| 2026-05-17 02:00 | Claude Opus 4.7 (Claude Code) | Desktop | **v2.0 layout overhaul** — header now sits above nav on all 4 pages; nav below header with full-width `border-bottom` separator; blue accent line rebuilt as a `::before` pseudo at `left:24px` so it locks to the same vertical rail as the Dashboard/All button left borders; all pages standardised to `max-width: 1600px` (was 1200/1400px) to stop the header jumping between pages; `header-inner`/`nav-inner` wrappers added; bumped to v2.0 (`index.html`, `charts.html`, `metrics.html`, `calculator.html`, `FEATURES.md`). |
| 2026-05-16 20:30 | Claude Sonnet 4.6 (Claude Code) | Desktop | Charts page: aligned header, toolbar, and sidebar to same left edge; centred toolbar; added proper header block; bumped to v1.9 (`charts.html`). |
| 2026-05-16 20:15 | Claude Sonnet 4.6 (Claude Code) | Desktop | Standardised headers across all 4 pages — `// AI Infrastructure` label, two-word h1 with cyan accent, consistent 11px label / 36px h1 / 13px sub / cyan border-left on all (`index.html`, `charts.html`, `calculator.html`, `metrics.html`). |
| 2026-05-16 20:00 | Claude Sonnet 4.6 (Claude Code) | Desktop | Added Stock Metrics page — market cap, beta, P/E, avg vol, 1D/1W/1M vol, YTD, div yield, short interest, analyst rating; sortable columns; category filter; search; tooltips on all metric headers; nav updated on all pages (`metrics.html`, `update_prices.py`, `index.html`, `charts.html`, `calculator.html`). |
| 2026-05-16 19:00 | Claude Sonnet 4.6 (Claude Code) | Desktop | Added Yahoo Finance links to ticker symbols on dashboard and calculator; RR. → RR.L and 285A → 285A.T suffixes; nav reordered to Dashboard → Metrics → Charts → What-If (`index.html`, `calculator.html`). |
| 2026-05-16 18:00 | Claude Sonnet 4.6 (Claude Code) | Desktop | Review fixes across all files — broken Dashboard nav link, dead stats-bar CSS, footer speculative disclaimer, PSTG name/fallback text, TLN exchange, bot docstring, dead price_ytd key, AGENTS.md stale refs, tzdata dep (`index.html`, `charts.html`, `calculator.html`, `update_prices.py`, `AGENTS.md`, `requirements.txt`). |
| 2026-05-16 17:15 | Claude Sonnet 4.6 (Claude Code) | Desktop | Fixed company names in calculator — static `NAMES` lookup replaces missing `s.name` field from `prices.json`; Company column now shows full names (`calculator.html`). |
| 2026-05-14 01:00 | Claude Sonnet 4.6 (Claude Code) | Desktop | Added What-If calculator page (`calculator.html`) — Jan 2026 investment simulation for £100/£1k/£10k per stock, sortable by YTD, totals row, search box, ticker tape; added to nav on all pages; bumped to v1.8 (`calculator.html`, `index.html`). |
| 2026-05-14 00:30 | Claude Sonnet 4.6 (Claude Code) | Desktop | Added ticker/company search box inline with stock count; search and category filters work together; fixed stock count to 48; bumped to v1.7 (`index.html`). |
| 2026-05-14 00:00 | Claude Sonnet 4.6 (Claude Code) | Desktop | Removed Risk and Revenue columns; added sortable YTD column beside 1yr Return; YTD pill moved out of price cell into dedicated column; bot now colours YTD span green/red; bumped to v1.6 (`index.html`, `update_prices.py`). |
| 2026-05-13 23:30 | Claude Sonnet 4.6 (Claude Code) | Desktop | Added YTD change pill — bot fetches first trading day of year price, calculates YTD %, adds pill alongside 1D/1W/1M; `data-change-ytd` attribute added; bumped to v1.5 (`update_prices.py`, `index.html`, `AGENTS.md`). |
| 2026-05-13 23:00 | Claude Sonnet 4.6 (Claude Code) | Desktop | Removed static category list and stats bar from header — cleaner layout; bumped to v1.4 (`index.html`). |
| 2026-05-13 22:30 | Claude Sonnet 4.6 (Claude Code) | Desktop | Added 1W and 1M change pills under price — bot fetches 1-month history, calculates % change, rebuilds price cell with coloured cpill spans; `data-change-1w` and `data-change-1m` attributes added; bumped to v1.3 (`update_prices.py`, `index.html`, `AGENTS.md`). |
| 2026-05-13 22:00 | Claude Sonnet 4.6 (Claude Code) | Desktop | Default sort by 1yr Return high to low on page load; bumped version to v1.2 (`index.html`). |
| 2026-05-13 21:30 | Claude Sonnet 4.6 (Claude Code) | Desktop | Added 11 missing tickers to `charts.html` across 4 new categories (Fibre/Optical, DSP/Semi, Test Equipment, Materials) — sidebar buttons, category grid, CSS vars, and toolbar buttons; bumped version to v1.1 (`charts.html`, `index.html`). |
| 2026-05-13 20:00 | Claude Sonnet 4.6 (Claude Code) | Desktop | Added `prices-data.js` global so ticker tape works without a server (file:// compatible); tape on all pages now reads `window.PRICES_DATA` with fetch + DOM fallbacks; workflow commits all three output files (`update_prices.py`, `index.html`, `charts.html`, `update-prices.yml`). |
| 2026-05-13 19:30 | Claude Sonnet 4.6 (Claude Code) | Desktop | Migrated ticker tape to `prices.json` — bot now writes JSON alongside HTML; tape on `index.html` fetches JSON with DOM fallback; tape added to `charts.html`; workflow updated to commit `prices.json` (`update_prices.py`, `index.html`, `charts.html`, `update-prices.yml`). |
| 2026-05-13 19:15 | Claude Sonnet 4.6 (Claude Code) | Desktop | Fixed ticker tape showing 0% — `data-change-1d` requires `dataset['change-1d']` bracket notation in JS, not `dataset.change1d` (`index.html`). |
| 2026-05-13 19:00 | Claude Sonnet 4.6 (Claude Code) | Desktop | Added scrolling ticker tape and daily change tracking — `data-change-1d` written by price bot; fixed-top 36px bar with pause-on-hover (`index.html`, `update_prices.py`). |
| 2026-05-13 18:00 | Claude Sonnet 4.6 (Claude Code) | Desktop | Added version display `v1` to header bar after last-updated timestamp (`index.html`). |
| 2026-05-13 17:47 | Claude Sonnet 4.6 (Claude Code) | Desktop | Added 4 new categories and 11 new tickers: Fibre / Optical (COHR, LITE, AAOI, FN), DSP / Semi (MTSI, SMTC, CRDO, MXL), Test Equipment (KEYS, VIAV), Materials (AXTI). New CSS variables, filter buttons, and category badge styles. Stock count 38 → 49 (`index.html`, `update_prices.py`). |

## Cross-AI editing notes

- Prepend **one new row** directly under the table header (above older rows).
- Same-day entries: newest session first; use finer ordering by **time** when needed.
- Use **BST** timestamps to stay consistent with `index.html` last-updated display.
